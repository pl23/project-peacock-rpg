import json

def load_json(selected_Save):
    with open(selected_Save, 'r') as file:
        data = json.load(file)
    return data

def seen_T(data):
    if data [''] == True:
        print ('not coded yet ')


    seen_1(data)
    
def seen_1(data):
    input('')
 
